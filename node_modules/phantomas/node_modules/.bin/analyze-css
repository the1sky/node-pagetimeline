#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../analyze-css/bin/analyze-css.js" "$@"
  ret=$?
else 
  node  "$basedir/../analyze-css/bin/analyze-css.js" "$@"
  ret=$?
fi
exit $ret
